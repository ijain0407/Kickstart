// TODO(B): confirm these against Person B's lesson IDs. Single source of truth for this workstream.
export const lessonIds = ['rules-basics', 'positions', 'formations', 'terms-slang', 'how-to-watch'];
// Culture/league questions live outside the five lessons; they only appear in quick quizzes.
export const cultureLessonId = 'leagues-culture';
export const allQuestionLessonIds = [...lessonIds, cultureLessonId];
