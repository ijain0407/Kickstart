{
  "name": "kickstart",
  "private": true,
  "type": "module",
  "description": "Bilingual soccer-learning platform: one API gateway, one frontend.",
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "node --watch server/index.js",
    "dev:web": "npm run dev --prefix frontend",
    "start": "node server/index.js",
    "build": "npm run build --prefix frontend",
    "smoke": "node server/smoke.js",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix league_feature && npm install --prefix quiz_feature && npm install --prefix person-b && npm install --prefix chatbot_feature",
    "crests": "node league_feature/scripts/link-crests.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
